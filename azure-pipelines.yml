trigger:
- master

resources:
- repo: self

stages:
- stage: Build
  displayName: Build image
  jobs:  
  - template: .build/azure-pipelines-job-build.yml
    parameters:
      DBCVER: 6.4.2
      DBCVERNODOTS: 642
